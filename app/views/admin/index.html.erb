<% t =Time.now %>
<% t.to_s %>

  <div class="container">
    <div class="info">
        <h1>Hello!</h1>
        Now is <strong><%= t.strftime "%d %B %Y %H:%M:%S" %></strong><br/>
        We have <%= pluralize(@total_orders, "order") %>.
    </div>
  </div>